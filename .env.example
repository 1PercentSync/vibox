# ViBox Environment Variables Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# REQUIRED CONFIGURATION
# =============================================================================

# API Token for authentication (REQUIRED)
# Generate a secure token: openssl rand -hex 32
API_TOKEN=your-secret-token-here

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================

# Server Configuration
# --------------------

# Port to listen on (default: 3000)
PORT=3000

# Host port for docker-compose (default: 3000)
HOST_PORT=3000

# Docker Configuration
# -------------------

# Docker socket path (default: unix:///var/run/docker.sock)
DOCKER_HOST=unix:///var/run/docker.sock

# Default image for workspaces (default: ubuntu:22.04)
DEFAULT_IMAGE=ubuntu:22.04

# Resource Limits
# --------------

# Memory limit for containers in bytes (default: 536870912 = 512MB)
MEMORY_LIMIT=536870912

# CPU limit for containers in nanoseconds (default: 1000000000 = 1 CPU)
CPU_LIMIT=1000000000

# =============================================================================
# EXAMPLE CONFIGURATIONS
# =============================================================================

# Development Example:
# --------------------
# API_TOKEN=dev-token-123456789
# PORT=3000
# DEFAULT_IMAGE=ubuntu:22.04
# MEMORY_LIMIT=536870912
# CPU_LIMIT=1000000000

# Production Example:
# ------------------
# API_TOKEN=$(openssl rand -hex 32)
# PORT=3000
# DEFAULT_IMAGE=ubuntu:22.04
# MEMORY_LIMIT=1073741824  # 1GB
# CPU_LIMIT=2000000000      # 2 CPUs

# =============================================================================
# NOTES
# =============================================================================

# 1. API_TOKEN is REQUIRED - the server will not start without it
# 2. Use openssl rand -hex 32 to generate a secure random token
# 3. Never commit .env file to version control
# 4. Resource limits are applied to created workspace containers
# 5. Restart the server after changing environment variables
