# Phase 2.5: Frontend-Backend Integration (前后端集成)

> **目标**: 将 React 前端嵌入到 Go 后端，实现单一可执行文件部署
>
> **状态**: 🔄 进行中
>
> **预计时间**: 1-2 天

---

## 目录

1. [概述](#概述)
2. [当前状态](#当前状态)
3. [集成方案](#集成方案)
4. [技术实现](#技术实现)
5. [部署架构](#部署架构)
6. [开发流程](#开发流程)
7. [测试计划](#测试计划)

---

## 概述

### 为什么需要 Phase 2.5？

Phase 1 和 Phase 2 完成后，我们有：
- ✅ **Go 后端** - 完整的 API 服务 (http://localhost:3000)
- ✅ **React 前端** - 完整的 Web 界面 (http://localhost:5173)

**问题**：
- 需要运行两个独立进程（后端 + Vite dev server）
- 部署时需要单独配置前后端
- 不是一个"整体"的应用

**解决方案**：Phase 2.5
- 将前端构建产物嵌入到 Go 后端
- 单一可执行文件包含前端 + 后端
- 一个命令启动整个应用

### 核心目标

1. **单一可执行文件** - `vibox.exe` 或 `vibox` 包含所有资源
2. **简化部署** - 无需单独部署前端，无需 Nginx/Caddy（开发环境）
3. **生产就绪** - 优化的静态资源服务，SPA 路由支持
4. **开发友好** - 保持前后端分离开发模式

---

## 当前状态

### 已完成（Phase 1 & 2）

**后端 (Phase 1)**:
- ✅ RESTful API (`/api/*`)
- ✅ WebSocket 终端 (`/ws/*`)
- ✅ 端口转发 (`/forward/*`)
- ✅ Cookie 鉴权
- ✅ 数据持久化

**前端 (Phase 2)**:
- ✅ React 应用 (Vite + TypeScript)
- ✅ 8 个模块完成
- ✅ 生产构建优化 (~250KB gzipped)
- ✅ 代码分割和 vendor chunking

### 待完成（Phase 2.5）

- ⏳ 前端静态文件嵌入
- ⏳ 路由器更新（静态文件服务）
- ⏳ SPA fallback 路由
- ⏳ 完整集成测试
- ⏳ 部署文档更新

---

## 集成方案

### 方案对比

| 方案 | 优点 | 缺点 | 选择 |
|------|------|------|------|
| **Nginx/Caddy 反向代理** | 标准方案，性能好 | 需要额外配置，部署复杂 | ❌ 不选 |
| **Go embed + Gin 静态服务** | 单一文件，部署简单 | Go 静态服务性能略低 | ✅ **选择** |
| **前端单独部署** | 解耦，灵活 | 部署复杂，CORS 问题 | ❌ 不选 |

### 选择的方案：Go embed

**技术**: Go 1.16+ `embed` 包

**原理**:
1. 前端构建 → `frontend/dist/` 目录
2. Go `embed` → 将 `dist/` 嵌入到二进制文件
3. Gin 路由 → 提供静态文件服务
4. SPA fallback → 所有未匹配路由返回 `index.html`

**优势**:
- ✅ 单一可执行文件
- ✅ 无需额外依赖
- ✅ 跨平台支持
- ✅ 简化部署流程

---

## 技术实现

### 1. 前端构建

```bash
cd frontend
npm run build
```

**输出** (`frontend/dist/`):
```
dist/
├── index.html                    # 入口文件
└── assets/
    ├── index-*.js                # 主应用 (~68KB gzipped)
    ├── react-vendor-*.js         # React (~31KB gzipped)
    ├── xterm-vendor-*.js         # xterm.js (~98KB gzipped)
    ├── ui-vendor-*.js            # UI 库 (~27KB gzipped)
    ├── icons-vendor-*.js         # 图标 (~2KB gzipped)
    ├── WorkspacesPage-*.js       # 工作空间页面
    ├── WorkspaceDetailPage-*.js  # 详情页面
    └── *.css                     # 样式文件 (~7KB gzipped)
```

**总大小**: ~250KB (gzipped)

---

### 2. 创建静态文件嵌入包

**文件**: `internal/static/embed.go`

```go
package static

import "embed"

// StaticFiles embeds the frontend build output
// This uses Go 1.16+ embed feature to include static files in the binary
//go:embed dist
var StaticFiles embed.FS
```

**说明**:
- `//go:embed dist` - 编译指令，嵌入 `frontend/dist` 目录
- `embed.FS` - 文件系统接口，可以像普通文件系统一样访问

---

### 3. 更新路由器

**文件**: `internal/api/router.go`

**需要添加**:

```go
import (
    "io/fs"
    "net/http"
    "github.com/1PercentSync/vibox/internal/static"
)

func SetupRouter(...) *gin.Engine {
    // ... 现有路由 ...

    // Serve static files from embedded filesystem
    staticFS, _ := fs.Sub(static.StaticFiles, "dist")
    router.Use(func(c *gin.Context) {
        // Skip API, WebSocket, and forward routes
        if strings.HasPrefix(c.Request.URL.Path, "/api") ||
           strings.HasPrefix(c.Request.URL.Path, "/ws") ||
           strings.HasPrefix(c.Request.URL.Path, "/forward") ||
           strings.HasPrefix(c.Request.URL.Path, "/health") {
            c.Next()
            return
        }

        // Try to serve static file
        if _, err := staticFS.Open(strings.TrimPrefix(c.Request.URL.Path, "/")); err == nil {
            c.FileFromFS(c.Request.URL.Path, http.FS(staticFS))
            c.Abort()
            return
        }

        // SPA fallback: serve index.html for all other routes
        c.FileFromFS("/index.html", http.FS(staticFS))
        c.Abort()
    })

    return router
}
```

**路由优先级**:
1. `/api/*` - API 路由（最高优先级）
2. `/ws/*` - WebSocket 路由
3. `/forward/*` - 端口转发路由
4. `/health` - 健康检查
5. 静态文件 - 如果文件存在
6. SPA fallback - 返回 `index.html`

---

### 4. 构建流程

**开发模式** (前后端分离):
```bash
# Terminal 1: 后端
export API_TOKEN=dev-token-123
go run ./cmd/server

# Terminal 2: 前端 (带 HMR)
cd frontend
npm run dev
```

**生产模式** (单一可执行文件):
```bash
# 1. 构建前端
cd frontend
npm run build
cd ..

# 2. 构建后端（自动嵌入前端）
go build -o vibox ./cmd/server

# 3. 运行
export API_TOKEN=your-secret-token
./vibox
```

**跨平台构建**:
```bash
# Windows
GOOS=windows GOARCH=amd64 go build -o vibox.exe ./cmd/server

# Linux
GOOS=linux GOARCH=amd64 go build -o vibox ./cmd/server

# macOS
GOOS=darwin GOARCH=amd64 go build -o vibox ./cmd/server
```

---

## 部署架构

### 开发环境架构（前后端分离）

```
┌─────────────────────────────────────────────────┐
│  Developer Machine                              │
│                                                 │
│  Terminal 1:                Terminal 2:        │
│  ┌───────────────┐          ┌───────────────┐ │
│  │ Go Backend    │          │ Vite Dev      │ │
│  │ :3000         │◄─────────│ Server :5173  │ │
│  │               │  Proxy   │               │ │
│  │ /api/*        │  /api → │ React + HMR   │ │
│  │ /ws/*         │  :3000   │               │ │
│  │ /forward/*    │          │               │ │
│  └───────────────┘          └───────────────┘ │
└─────────────────────────────────────────────────┘
```

**特点**:
- ✅ 前端热更新 (HMR)
- ✅ 快速开发迭代
- ✅ 独立调试前后端

---

### 生产环境架构（单一可执行文件）

```
┌────────────────────────────────────────────┐
│  Production Server                         │
│                                            │
│  ┌──────────────────────────────────────┐ │
│  │  vibox (单一可执行文件)              │ │
│  │                                      │ │
│  │  ┌────────────────┐                 │ │
│  │  │ Gin Router     │                 │ │
│  │  │                │                 │ │
│  │  │ /              │─► Static (React)│ │
│  │  │ /api/*         │─► API Handler   │ │
│  │  │ /ws/*          │─► WebSocket     │ │
│  │  │ /forward/*     │─► Proxy         │ │
│  │  └────────────────┘                 │ │
│  │                                      │ │
│  │  [Frontend static files embedded]   │ │
│  └──────────────────────────────────────┘ │
│                 :3000                      │
└────────────────────────────────────────────┘
                  │
                  ▼
         ┌────────────────┐
         │  Caddy/Nginx   │  (可选反向代理)
         │  (HTTPS)       │
         └────────────────┘
                  │
                  ▼
              Internet
```

**特点**:
- ✅ 单一进程
- ✅ 简化部署
- ✅ 无需 Node.js 运行时
- ✅ 小文件大小 (~15-20MB 包含所有资源)

---

## 开发流程

### 前端开发

**场景**: 修改前端代码（组件、样式、逻辑）

```bash
# 1. 启动后端
export API_TOKEN=dev-token-123
go run ./cmd/server

# 2. 启动前端（带 HMR）
cd frontend
npm run dev

# 3. 访问 http://localhost:5173
# 4. 修改代码，浏览器自动刷新
```

**优势**: 保持 Vite HMR 的快速开发体验

---

### 后端开发

**场景**: 修改后端代码（API、服务、中间件）

```bash
# 1. 启动前端（Mock 或真实后端）
cd frontend
npm run dev

# 2. 修改后端代码
# 3. 重启后端
go run ./cmd/server
```

---

### 完整测试

**场景**: 测试生产构建，验证集成

```bash
# 1. 构建前端
cd frontend
npm run build

# 2. 运行后端（自动加载嵌入的前端）
cd ..
go run ./cmd/server

# 3. 访问 http://localhost:3000
# 4. 测试所有功能
```

---

## 测试计划

### 功能测试

1. **静态文件服务**
   - [ ] 访问 `/` 显示登录页面
   - [ ] CSS 和 JS 资源正确加载
   - [ ] 图标和字体正确显示

2. **SPA 路由**
   - [ ] 直接访问 `/workspace/xxx` 不显示 404
   - [ ] 前端路由切换正常
   - [ ] 刷新页面不丢失状态

3. **API 集成**
   - [ ] `/api/auth/login` 登录成功
   - [ ] `/api/workspaces` 列表正常
   - [ ] 创建、删除工作空间正常

4. **WebSocket 终端**
   - [ ] WebSocket 连接成功
   - [ ] 终端输入输出正常
   - [ ] 断线重连正常

5. **端口转发**
   - [ ] `/forward/:id/:port/*` 代理正常
   - [ ] Cookie 认证正常
   - [ ] 容器服务访问正常

---

### 性能测试

1. **文件大小**
   - [ ] 可执行文件 < 25MB
   - [ ] 初始加载 < 300KB (gzipped)

2. **启动时间**
   - [ ] 服务器启动 < 2秒
   - [ ] 首次页面加载 < 1秒

3. **内存使用**
   - [ ] 空闲内存 < 50MB
   - [ ] 10个工作空间 < 200MB

---

### 兼容性测试

1. **操作系统**
   - [ ] Windows 10/11
   - [ ] Ubuntu 20.04/22.04
   - [ ] macOS 12+

2. **浏览器**
   - [ ] Chrome/Edge 90+
   - [ ] Firefox 88+
   - [ ] Safari 14+

---

## 文件结构变化

### 新增文件

```
internal/
└── static/
    └── embed.go          # 静态文件嵌入包

frontend/
└── dist/                 # 构建产物（git ignored）
    ├── index.html
    └── assets/
```

### 修改文件

```
internal/api/router.go    # 添加静态文件服务
.gitignore                # 添加 frontend/dist/
```

### 不变文件

```
cmd/server/main.go        # 无需修改
internal/service/*        # 无需修改
internal/handler/*        # 无需修改
```

---

## 依赖要求

### 开发环境

- Go 1.21+ (需要 embed 支持)
- Node.js 18+ (前端构建)
- Docker (容器管理)

### 生产环境

- Docker (容器管理)
- 无需 Node.js (前端已嵌入)
- 无需 Nginx/Caddy (可选)

---

## 后续优化（可选）

### Phase 2.5 完成后可选优化

1. **Gzip 压缩** - 添加 Gin gzip 中间件
2. **缓存控制** - 添加 Cache-Control headers
3. **CDN 支持** - 支持前端资源 CDN 部署
4. **多环境构建** - 支持 dev/staging/prod 环境变量

这些优化可以在 Phase 3 中根据需要实现。

---

## 成功标准

Phase 2.5 完成后，应该能够：

- ✅ 运行单一命令启动完整应用
- ✅ 访问 http://localhost:3000 显示前端
- ✅ 所有 API 功能正常工作
- ✅ WebSocket 终端正常工作
- ✅ 端口转发正常工作
- ✅ 生产构建 < 25MB
- ✅ 支持 Windows/Linux/macOS
- ✅ 部署文档完整准确

---

## 参考资源

- [Go embed 官方文档](https://pkg.go.dev/embed)
- [Gin 静态文件服务](https://gin-gonic.com/docs/examples/serving-static-files/)
- [Vite 生产构建](https://vitejs.dev/guide/build.html)

---

**下一步**: 开始实施 Phase 2.5 集成方案

---

**文档版本**: v1.0
**创建日期**: 2025-11-11
**状态**: 进行中
