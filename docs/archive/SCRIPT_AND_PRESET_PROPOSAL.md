# Script Library & Workspace Presets - 方案建议

> **目标**：为 ViBox 添加脚本库和工作空间预设功能，提升用户体验和工作效率
>
> **状态**：⏸️ **暂缓实施** - 待后续阶段再考虑
>
> **日期**：2025-11-10
>
> **决策**：此功能为可选增强功能，暂不纳入当前开发计划。优先完成核心功能（第一阶段后端 + 第二阶段前端）。

---

## ⚠️ 重要说明

**此文档仅作为未来参考方案保留，当前不会实施。**

如需重新考虑此功能，请参考本文档进行评估和实施。

---

## 目录

1. [需求概述](#需求概述)
2. [功能设计](#功能设计)
3. [数据模型](#数据模型)
4. [API 设计](#api-设计)
5. [前端界面设计](#前端界面设计)
6. [实现计划](#实现计划)
7. [优缺点分析](#优缺点分析)
8. [决策建议](#决策建议)

---

## 需求概述

### 1. 脚本库（Script Library）

**背景**：
用户经常需要重复使用相同的初始化脚本（如安装 Node.js、配置 Git、安装常用工具等），每次创建工作空间都需要重新输入脚本内容。

**需求**：
- ✅ 持久化保存常用脚本
- ✅ 为脚本命名和分类
- ✅ 创建工作空间时可以引用已保存的脚本
- ✅ 也可以临时写脚本（不保存到脚本库）
- ✅ 支持编辑、删除已保存的脚本

**使用场景**：
```
用户保存脚本：
1. 创建脚本 "install-node"
2. 保存到脚本库

创建工作空间时：
1. 从脚本库选择 "install-node"
2. 临时添加脚本 "setup-project"（不保存到库）
3. 创建工作空间
```

### 2. 工作空间预设（Workspace Presets）

**背景**：
用户可能有多个常用的工作空间配置（如前端开发环境、后端开发环境、数据分析环境等），每次都需要手动配置镜像、脚本、端口。

**需求**：
- ✅ 保存完整的工作空间配置模板
- ✅ 包含镜像、脚本、端口映射
- ✅ 快速基于预设创建工作空间
- ✅ 支持编辑、删除预设

**使用场景**：
```
用户创建预设：
1. 定义 "React 开发环境" 预设
   - 镜像: node:20
   - 脚本: install-node, install-react-cli
   - 端口: 3000: "React Dev Server", 5173: "Vite"
2. 保存预设

快速创建工作空间：
1. 选择 "React 开发环境" 预设
2. 输入工作空间名称
3. 一键创建
```

---

## 功能设计

### 方案对比

| 功能 | 脚本库 | 预设 |
|------|--------|------|
| **粒度** | 单个脚本 | 完整配置 |
| **复用性** | 高（可组合使用） | 中（整套配置） |
| **灵活性** | 高（自由组合） | 低（固定配置） |
| **使用场景** | 常用脚本片段 | 常用工作环境 |
| **适合用户** | 高级用户 | 所有用户 |

### 推荐方案：两者都实现 ✅

**原因**：
1. **脚本库**是基础，提供灵活性
2. **预设**是快捷方式，提升效率
3. 两者配合使用，体验最佳

**优先级**：
1. **Phase 1**：脚本库（基础功能）
2. **Phase 2**：预设（基于脚本库构建）

---

## 数据模型

### 1. 脚本库（Script Library）

```go
// internal/domain/script_library.go
type SavedScript struct {
    ID          string    `json:"id"`           // 脚本 ID（唯一）
    Name        string    `json:"name"`         // 脚本名称
    Description string    `json:"description"`  // 脚本描述
    Content     string    `json:"content"`      // 脚本内容
    Tags        []string  `json:"tags"`         // 标签（如 "node", "git", "docker"）
    CreatedAt   time.Time `json:"created_at"`
    UpdatedAt   time.Time `json:"updated_at"`
}

// 脚本库数据结构
type ScriptLibrary struct {
    Scripts map[string]*SavedScript `json:"scripts"` // key=脚本ID
}
```

**存储位置**：`data/scripts.json`

**存储示例**：
```json
{
  "scripts": {
    "script-001": {
      "id": "script-001",
      "name": "install-node",
      "description": "Install Node.js 20 LTS",
      "content": "#!/bin/bash\ncurl -fsSL https://deb.nodesource.com/setup_20.x | bash -\napt-get install -y nodejs",
      "tags": ["node", "javascript"],
      "created_at": "2025-11-10T12:00:00Z",
      "updated_at": "2025-11-10T12:00:00Z"
    },
    "script-002": {
      "id": "script-002",
      "name": "setup-git",
      "description": "Configure Git user",
      "content": "#!/bin/bash\ngit config --global user.name \"Your Name\"\ngit config --global user.email \"you@example.com\"",
      "tags": ["git"],
      "created_at": "2025-11-10T12:05:00Z",
      "updated_at": "2025-11-10T12:05:00Z"
    }
  }
}
```

### 2. 工作空间预设（Workspace Presets）

```go
// internal/domain/preset.go
type WorkspacePreset struct {
    ID          string              `json:"id"`          // 预设 ID（唯一）
    Name        string              `json:"name"`        // 预设名称
    Description string              `json:"description"` // 预设描述
    Icon        string              `json:"icon"`        // 图标（可选）
    Config      PresetConfig        `json:"config"`      // 配置
    CreatedAt   time.Time           `json:"created_at"`
    UpdatedAt   time.Time           `json:"updated_at"`
}

type PresetConfig struct {
    Image   string        `json:"image"`   // Docker 镜像
    Scripts []ScriptRef   `json:"scripts"` // 脚本引用
    Ports   map[string]string `json:"ports"` // 端口映射
}

type ScriptRef struct {
    Type    string `json:"type"`     // "library" 或 "inline"
    ID      string `json:"id"`       // 脚本库 ID（仅当 type=library）
    Name    string `json:"name"`     // 脚本名称
    Content string `json:"content"`  // 脚本内容（仅当 type=inline）
    Order   int    `json:"order"`    // 执行顺序
}

// 预设库数据结构
type PresetLibrary struct {
    Presets map[string]*WorkspacePreset `json:"presets"` // key=预设ID
}
```

**存储位置**：`data/presets.json`

**存储示例**：
```json
{
  "presets": {
    "preset-001": {
      "id": "preset-001",
      "name": "React 开发环境",
      "description": "Node.js + React + Vite 开发环境",
      "icon": "⚛️",
      "config": {
        "image": "node:20",
        "scripts": [
          {
            "type": "library",
            "id": "script-001",
            "name": "install-node",
            "order": 1
          },
          {
            "type": "inline",
            "name": "install-react-tools",
            "content": "#!/bin/bash\nnpm install -g create-react-app vite",
            "order": 2
          }
        ],
        "ports": {
          "3000": "React Dev Server",
          "5173": "Vite"
        }
      },
      "created_at": "2025-11-10T12:00:00Z",
      "updated_at": "2025-11-10T12:00:00Z"
    }
  }
}
```

### 3. 工作空间配置扩展

```go
// internal/domain/workspace.go (扩展)
type Workspace struct {
    ID          string                `json:"id"`
    Name        string                `json:"name"`
    ContainerID string                `json:"container_id,omitempty"`
    Status      WorkspaceStatus       `json:"status,omitempty"`
    CreatedAt   time.Time             `json:"created_at"`
    Config      WorkspaceConfig       `json:"config"`
    Ports       map[string]string     `json:"ports,omitempty"`
    PresetID    string                `json:"preset_id,omitempty"`  // 新增：记录使用的预设
    Error       string                `json:"error,omitempty"`
}

type WorkspaceConfig struct {
    Image   string      `json:"image"`
    Scripts []ScriptRef `json:"scripts"`  // 改为 ScriptRef（支持引用和内联）
}
```

---

## API 设计

### 1. 脚本库 API

#### 1.1 创建脚本

```http
POST /api/scripts
X-ViBox-Token: {token}
Content-Type: application/json

{
  "name": "install-node",
  "description": "Install Node.js 20 LTS",
  "content": "#!/bin/bash\n...",
  "tags": ["node", "javascript"]
}
```

**响应**：
```json
{
  "id": "script-001",
  "name": "install-node",
  "description": "Install Node.js 20 LTS",
  "content": "#!/bin/bash\n...",
  "tags": ["node", "javascript"],
  "created_at": "2025-11-10T12:00:00Z",
  "updated_at": "2025-11-10T12:00:00Z"
}
```

#### 1.2 列出所有脚本

```http
GET /api/scripts
X-ViBox-Token: {token}
```

**响应**：
```json
[
  {
    "id": "script-001",
    "name": "install-node",
    "description": "Install Node.js 20 LTS",
    "tags": ["node", "javascript"],
    "created_at": "2025-11-10T12:00:00Z"
  },
  ...
]
```

#### 1.3 获取脚本详情

```http
GET /api/scripts/:id
X-ViBox-Token: {token}
```

#### 1.4 更新脚本

```http
PUT /api/scripts/:id
X-ViBox-Token: {token}
Content-Type: application/json

{
  "name": "install-node-updated",
  "content": "#!/bin/bash\n..."
}
```

#### 1.5 删除脚本

```http
DELETE /api/scripts/:id
X-ViBox-Token: {token}
```

---

### 2. 预设 API

#### 2.1 创建预设

```http
POST /api/presets
X-ViBox-Token: {token}
Content-Type: application/json

{
  "name": "React 开发环境",
  "description": "Node.js + React + Vite",
  "icon": "⚛️",
  "config": {
    "image": "node:20",
    "scripts": [
      {
        "type": "library",
        "id": "script-001",
        "order": 1
      },
      {
        "type": "inline",
        "name": "install-react",
        "content": "npm install -g create-react-app",
        "order": 2
      }
    ],
    "ports": {
      "3000": "React Dev Server"
    }
  }
}
```

**响应**：
```json
{
  "id": "preset-001",
  "name": "React 开发环境",
  ...
}
```

#### 2.2 列出所有预设

```http
GET /api/presets
X-ViBox-Token: {token}
```

#### 2.3 基于预设创建工作空间

```http
POST /api/workspaces/from-preset
X-ViBox-Token: {token}
Content-Type: application/json

{
  "name": "my-react-project",
  "preset_id": "preset-001"
}
```

**后端逻辑**：
1. 从预设库读取预设配置
2. 解析脚本引用（从脚本库读取内容）
3. 创建工作空间
4. 记录 `preset_id`（方便用户知道是从哪个预设创建的）

---

## 前端界面设计

### 1. 脚本库管理页面

```
┌─────────────────────────────────────────────────┐
│  ViBox        Workspaces    Scripts    Presets  │
├─────────────────────────────────────────────────┤
│                                                 │
│  Script Library                  [+ New Script] │
│                                                 │
│  ┌──────────────┐  ┌──────────────┐           │
│  │ install-node │  │ setup-git    │           │
│  │ 🟢 node, js  │  │ 🟢 git       │           │
│  │              │  │              │           │
│  │ Install      │  │ Configure    │           │
│  │ Node.js 20   │  │ Git user     │           │
│  │              │  │              │           │
│  │ [Edit] [Del] │  │ [Edit] [Del] │           │
│  └──────────────┘  └──────────────┘           │
│                                                 │
└─────────────────────────────────────────────────┘
```

### 2. 预设管理页面

```
┌─────────────────────────────────────────────────┐
│  Workspace Presets               [+ New Preset] │
├─────────────────────────────────────────────────┤
│                                                 │
│  ┌──────────────────────────────────────────┐  │
│  │ ⚛️ React 开发环境                         │  │
│  │ Node.js + React + Vite                   │  │
│  │                                          │  │
│  │ Image: node:20                           │  │
│  │ Scripts: install-node, install-react     │  │
│  │ Ports: 3000, 5173                        │  │
│  │                                          │  │
│  │ [Use Preset]  [Edit]  [Delete]          │  │
│  └──────────────────────────────────────────┘  │
│                                                 │
│  ┌──────────────────────────────────────────┐  │
│  │ 🐍 Python 开发环境                        │  │
│  │ Python + Jupyter + NumPy                 │  │
│  │                                          │  │
│  │ [Use Preset]  [Edit]  [Delete]          │  │
│  └──────────────────────────────────────────┘  │
│                                                 │
└─────────────────────────────────────────────────┘
```

### 3. 创建工作空间（增强版）

```
┌───────────────────────────────────────┐
│  Create New Workspace            [X]  │
├───────────────────────────────────────┤
│                                       │
│  ○ From Preset                        │
│  ┌─────────────────────────────────┐ │
│  │ [Select Preset ▼]               │ │
│  └─────────────────────────────────┘ │
│                                       │
│  ● Custom Configuration               │
│                                       │
│  Workspace Name *                     │
│  [_____________________________]      │
│                                       │
│  Docker Image                         │
│  [ubuntu:22.04          ▼]            │
│                                       │
│  ┌─ Scripts ──────────────────────┐  │
│  │                                │  │
│  │ [+ From Library] [+ Inline]   │  │
│  │                                │  │
│  │ ✓ install-node (Library)      │  │
│  │   Order: 1       [Remove]     │  │
│  │                                │  │
│  │ ✓ setup-project (Inline)      │  │
│  │   Order: 2       [Edit] [Del] │  │
│  │   ┌──────────────────────────┐│  │
│  │   │#!/bin/bash               ││  │
│  │   │echo "Setting up..."      ││  │
│  │   └──────────────────────────┘│  │
│  └────────────────────────────────┘  │
│                                       │
│  ┌─ Port Labels ──────────────────┐  │
│  │ 8080: [VS Code Server]         │  │
│  │ [+ Add Port]                   │  │
│  └────────────────────────────────┘  │
│                                       │
│        [Cancel]    [Create]           │
└───────────────────────────────────────┘
```

---

## 实现计划

### Phase 1: 脚本库（2-3 天）

**任务清单**：
- [ ] 数据模型定义（`SavedScript`, `ScriptLibrary`）
- [ ] Repository 层实现（`ScriptRepository`）
- [ ] Service 层实现（`ScriptService`）
- [ ] API Handler 实现（CRUD 接口）
- [ ] 路由注册
- [ ] 前端页面：脚本库列表
- [ ] 前端页面：创建/编辑脚本对话框
- [ ] 创建工作空间时集成脚本选择

**验收标准**：
- ✅ 可以创建、编辑、删除脚本
- ✅ 创建工作空间时可以选择脚本库中的脚本
- ✅ 可以混合使用库脚本和内联脚本
- ✅ 脚本持久化保存

### Phase 2: 工作空间预设（2-3 天）

**任务清单**：
- [ ] 数据模型定义（`WorkspacePreset`, `PresetConfig`）
- [ ] Repository 层实现（`PresetRepository`）
- [ ] Service 层实现（`PresetService`）
- [ ] API Handler 实现（CRUD + 从预设创建工作空间）
- [ ] 路由注册
- [ ] 前端页面：预设列表
- [ ] 前端页面：创建/编辑预设对话框
- [ ] 创建工作空间时集成预设选择
- [ ] 内置预设（React、Python、Go 等）

**验收标准**：
- ✅ 可以创建、编辑、删除预设
- ✅ 可以基于预设快速创建工作空间
- ✅ 预设可以引用脚本库中的脚本
- ✅ 预设持久化保存

### Phase 3: 优化与扩展（1-2 天）

**任务清单**：
- [ ] 脚本标签过滤
- [ ] 预设搜索和分类
- [ ] 预设图标/配色自定义
- [ ] 导入/导出预设（JSON 文件）
- [ ] 内置预设模板

---

## 优缺点分析

### 脚本库

**优点**：
- ✅ 减少重复输入，提升效率
- ✅ 脚本可复用，灵活组合
- ✅ 方便维护和版本管理
- ✅ 支持标签分类，便于查找

**缺点**：
- ⚠️ 增加系统复杂度
- ⚠️ 需要额外的存储和管理
- ⚠️ 用户需要学习新功能

**适用场景**：
- 经常使用相同脚本的用户
- 需要维护多个工作空间的用户
- 团队共享脚本配置（未来扩展）

### 工作空间预设

**优点**：
- ✅ 快速创建常用环境，极大提升效率
- ✅ 降低配置门槛，新手友好
- ✅ 标准化工作环境
- ✅ 支持团队共享预设（未来扩展）

**缺点**：
- ⚠️ 灵活性低于手动配置
- ⚠️ 需要维护预设库
- ⚠️ 可能造成预设过多难以管理

**适用场景**：
- 有固定工作环境的用户
- 新手用户（可提供内置预设）
- 团队标准化环境（未来扩展）

---

## 决策建议

### 方案 A：只实现脚本库 ⭐⭐⭐

**适合**：
- 用户群体以高级用户为主
- 需要高灵活性
- 资源有限，优先实现核心功能

**工作量**：2-3 天

### 方案 B：只实现预设 ⭐⭐

**适合**：
- 用户群体以新手为主
- 需要快速上手
- 简化用户操作

**工作量**：2-3 天

**问题**：预设无法引用脚本库，只能内联脚本，维护成本高

### 方案 C：两者都实现 ⭐⭐⭐⭐⭐ （推荐）

**适合**：
- 兼顾所有用户群体
- 提供最佳用户体验
- 长期发展考虑

**工作量**：4-6 天

**实施顺序**：
1. **Phase 1**：脚本库（基础）
2. **Phase 2**：预设（基于脚本库）

**优势**：
- 脚本库提供灵活性
- 预设提供便捷性
- 两者配合使用，体验最佳
- 可以先实现脚本库，预设后续扩展

---

## 数据文件结构

```
data/
├── workspaces.json   # 工作空间配置（已有）
├── scripts.json      # 脚本库（新增）
└── presets.json      # 预设库（新增）
```

---

## 内置预设建议

如果实现预设功能，建议提供以下内置预设：

| 预设名称 | 镜像 | 用途 |
|---------|------|------|
| **Node.js 开发** | `node:20` | JavaScript/TypeScript 开发 |
| **React 开发** | `node:20` | React 应用开发 |
| **Python 开发** | `python:3.11` | Python 应用开发 |
| **Go 开发** | `golang:1.25` | Go 应用开发 |
| **数据科学** | `jupyter/datascience-notebook` | Jupyter + NumPy + Pandas |
| **Web 全栈** | `ubuntu:22.04` | Node + Python + Database |

---

## 未来扩展

### 1. 团队共享（第三阶段）

- 脚本库和预设支持团队共享
- 权限管理（公开/私有）
- 版本控制

### 2. 市场（第三阶段）

- 社区脚本和预设市场
- 用户可以发布和下载
- 评分和评论系统

### 3. 高级功能

- 脚本变量支持（如用户名、邮箱占位符）
- 脚本依赖关系
- 预设模板继承

---

## 总结

### 推荐方案：**方案 C - 两者都实现** ⭐⭐⭐⭐⭐

**理由**：
1. **脚本库**是基础功能，提供灵活性，工作量适中
2. **预设**基于脚本库构建，提升用户体验
3. 两者配合使用，满足不同用户需求
4. 可以分阶段实施，风险可控

**实施建议**：
1. **先实现脚本库**（Phase 1，2-3 天）
   - 验证用户反馈
   - 为预设打基础
2. **再实现预设**（Phase 2，2-3 天）
   - 基于脚本库构建
   - 提供内置预设

**总工作量**：4-6 天

---

**下一步**：
1. 用户决策是否实施
2. 如果实施，确定优先级和时间安排
3. 开始详细设计和实现

---

**作者**：Claude
**日期**：2025-11-10
**版本**：v1.0.0
